node_modules/
dist/
# agent-runner ships as a JS GitHub Action — its bundled dist/ MUST be committed
# (GitHub runs action.yml `main: dist/index.js` as-is, with no build step).
!agent-runner/dist/
!agent-runner/dist/**
build/
.next/
out/
coverage/
*.tsbuildinfo
.env
.env.local
.DS_Store
.idea/
.turbo/
.pnpm-store/
*.log
.devdigest/cache/
clones/
playwright-report/
test-results/
client/pnpm-workspace.yaml
dev-digest.iml
server/pnpm-workspace.yaml

# emdash worktree config (local)
.emdash.json

/dev-digest.iml

# pr-self-review local verdict (machine-local, do not commit)
.claude/pr-self-review.json
