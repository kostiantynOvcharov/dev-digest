# Insights — server

Non-obvious findings and gotchas. Add an entry whenever something surprised you,
so the next agent/session doesn't relearn it. Append-only — see the
`engineering-insights` skill for how entries are captured.

## What Works

- **2026-06-27** — To assert that a prompt section (skills/memory/etc.) actually lands in a review, drive a real run through `MockLLMProvider` and read it back from the trace rather than unit-testing the assembler: `POST /pulls/:id/review` → `waitForPrRuns(db, prId, { expected: 1 })` → `GET /runs/:runId/trace` → `prompt_assembly.skills`. Set the agent's `repo_intel:false` so context enrichment is skipped and the assertion is stable. Evidence: `server/test/skills.it.test.ts`.
- **2026-06-27** — For LLM-extraction features (conventions), apply the same "don't trust the model's citation" gate reviewer-core uses for findings: have the model return its `evidence_snippet`, but in the grounding gate verify the file exists under `repo.clonePath` + the cited line range is in-bounds + the model's quoted text actually appears there (normalized whitespace), then RE-DERIVE the stored snippet from the real file lines and DISCARD the model's text. Candidates failing any check are dropped — keep this pure (no I/O) so it's unit-testable without Docker. Evidence: `server/src/modules/conventions/helpers.ts` (`groundCandidates`), mirrors `reviewer-core/src/grounding.ts`.

## What Doesn't Work

- **2026-06-27** — Dependency installs MUST use pnpm, not npm. `npm install <pkg>` fails on this repo: first `ERESOLVE` (pre-existing peer-dep conflict), then even `--legacy-peer-deps` dies with `Cannot read properties of null (reading 'matches')`. `pnpm add <pkg>` works cleanly (a `pnpm-lock.yaml` is the real lockfile; the `npm run …` strings in older notes refer to scripts, not installs). Evidence: `server/pnpm-lock.yaml`, `README.md` (pnpm dev).
- **2026-06-27** — Do NOT apply the "context enrichment is best-effort, swallow & continue" rule to a PRIMARY user action. The conventions `extract` originally wrapped its LLM call in `catch { return [] }`; with no OpenAI key configured this turned a `ConfigError('OPENAI_API_KEY is not configured')` into a `200 []`, so the UI showed a blank "nothing extracted" page with zero clue. Best-effort/silent is only correct when the failed thing is OPTIONAL enrichment of some other deliverable. A user-triggered action whose whole purpose is the result must surface the error (rethrow `AppError`s as-is, wrap unexpected ones in `ExternalServiceError`); reserve `[]` for the genuine "ran fine, found nothing". Evidence: `server/src/modules/conventions/service.ts` (`extract`).
- **2026-07-05** — The persistent Blast path only surfaces a caller when TWO conditions both hold, or it silently vanishes: (1) `references.decl_file` is populated AND ∈ the changed files, and (2) the caller's file has a `file_rank` row. `getResolvedCallers` INNER-JOINs `file_rank` on the caller's `from_path`, so a real caller in an unranked file is dropped, not just unranked; and `insertReferences` writes `decl_file = NULL` (a separate resolve step backfills it), so a reference inserted via that path counts as a caller only AFTER resolution. Consequence for tests/seeding: insert references directly with `declFile` set AND insert a `file_rank` row per caller file — otherwise the blast comes back empty despite symbols+references existing. Evidence: `server/src/modules/repo-intel/repository.ts:503` (getResolvedCallers join), `:280` (insertReferences leaves decl_file null), `server/test/blast.it.test.ts`.
- **2026-06-28** — A path classifier with an "everything else → core" default is a JUNK-DRAWER trap. The Smart Diff `classifyFile` originally enumerated only narrow lock/dist/config/entry patterns and defaulted the rest to `core` — on a real lesson PR (138 files) that put 85 in "Core logic / review closely", led by `docs/…/experiment.md`: 33 `.md`, plus `.claude/` tooling, `CLAUDE.md`, `.gitignore`, `.sh`, `.json`. Fix: explicitly route every non-business-code family OUT of core — docs/markdown/assets (`.md`, `docs/`, root `README/CLAUDE/AGENTS/LICENSE…`, images) → boilerplate; dot-files/dot-dirs (`.claude/`, `.gitignore`, `.husky/`), shell scripts, and `.json` → wiring. Order matters: match boilerplate BEFORE wiring so a `.md` inside a dot-dir (`.claude/skills/x.md`) stays boilerplate, not wiring. After the fix core dropped 85→43 (only `.ts/.tsx`). Lesson: when a bucket is the fall-through default, audit a large real diff's bucket distribution — don't trust the happy-path examples. Evidence: `server/src/modules/pulls/classifier.constants.ts`, `classifier.test.ts`.

## Codebase Patterns

- **2026-06-14** — Shared contracts (`@devdigest/shared`) are vendored as TWO hand-maintained copies — `server/src/vendor/shared/` and `client/src/vendor/shared/` — resolved by tsconfig path alias, NOT auto-synced. Adding a field means editing both in lock-step; the only diffs between copies are comments. Evidence: `server/src/vendor/shared/contracts/trace.ts`, `platform.ts`.
- **2026-06-28** — SUPERSEDES the "only diffs are comments" claim above: the two vendored copies are NOT strictly identical-modulo-comments. `server/src/vendor/shared/contracts/knowledge.ts` exports `AgentVersionConfig` + `AgentVersion` that the `client/` copy OMITS entirely (server-only contracts the client never consumes). So don't assume a diff of the two files is empty when reconciling fields — the client copy is a SUBSET; add a field to the client copy only if the client actually needs it, and expect legitimate server-only exports. Evidence: `diff server/src/vendor/shared/contracts/knowledge.ts client/src/vendor/shared/contracts/knowledge.ts` (AgentVersion block, server-only).
- **2026-06-14** — PR-list per-PR aggregates (score, cost) are computed ON READ in `GET /repos/:id/pulls` via one `inArray` query + JS grouping, never denormalized onto `pull_requests`. "Latest review batch" cost has no batch id in the schema — approximated by summing `agent_runs.cost_usd` within a 120s window of the PR's newest priced run. Evidence: `server/src/modules/pulls/routes.ts`.
- **2026-06-14** — `completeAgentRun`'s `values` shape is declared in TWO places that must match: the repo fn (`repository/run.repo.ts`) AND the interface wrapper (`repository.ts:151`). Adding a field (e.g. `costUsd`) needs both or typecheck fails.
- **2026-06-27** — The backend is MIXED-architecture, not uniformly onion. `agents`/`repos`/`reviews`/`repo-intel` are fully layered (routes→service→repository); `settings`/`pulls`/`polling`/`workspace` are THIN — their `routes.ts` query Drizzle inline (no service/repo). Thin modules are grandfathered as warn-level in `arch:check` (`route-direct-db-legacy`); do NOT copy them — new modules must be layered. Evidence: `server/src/modules/{settings,pulls,polling,workspace}/routes.ts` vs `modules/agents/`.
- **2026-06-27** — `platform/container.ts` is the composition root and is deliberately the ONLY place allowed to import across all layers (adapters + sibling-module repos + module services). No `dependency-cruiser` rule uses `^src/platform` as a `from`, so this is by design — don't "fix" it, and don't copy the pattern into a module. Evidence: `server/.dependency-cruiser.cjs`, `server/src/platform/container.ts:26-31`.
- **2026-06-27** — The "skills" feature is pre-scaffolded everywhere EXCEPT one server wire: tables (`skills`/`skill_versions`/`agent_skills`) + contracts + `agents` link routes/repo + `reviewer-core` `assemblePrompt` (`## Skills / rules` block + `prompt_assembly.skills` trace slot) all already exist. `ReviewInput.skills?: string[]` is plumbed through `reviewPullRequest` but the server NEVER populates it — the single missing injection point is `runOneAgent`, just before the `reviewPullRequest(...)` call. Resolve via `agentsRepo.linkedSkills(agentId)` (already ordered by `order`), filter `skill.enabled` (global master switch), map to bodies. Evidence: `server/src/modules/reviews/run-executor.ts:191`.
- **2026-06-27** — The skills Stats tab needs "agents using this skill" (`GET /skills/:id/agents → Agent[]`). Do NOT reach for `agents/helpers.ts` `toAgentDto` from the skills module — a module→module import trips `arch:check`. Instead do the `agent_skills`→`agents` join inside `SkillsRepository` (a repo reading a sibling TABLE is fine; importing a sibling module's CODE is not) and map rows to `Agent` with a local mapper. Same rule blocked reusing `agents` link-state across modules. Evidence: `server/.dependency-cruiser.cjs`, `server/specs/skills.md` §6.
- **2026-06-27** — The "conventions" feature follows the same pre-scaffolded-but-unwired shape as skills: the `conventions` TABLE (`db/schema/knowledge.ts`), the `ConventionCandidate` vendored contract, a `conventions` entry in the `FeatureModelId` registry (`vendor/shared/contracts/platform.ts` — so the model is chosen via `resolveFeatureModel(container, ws, 'conventions')`, NEVER hardcoded), and even the client i18n (`client/messages/en/conventions.json`) all shipped before any extraction route/service/UI existed. When a lab/lesson names a feature, grep the schema + `FeatureModelId` + i18n first — the rails are usually already laid. Evidence: `server/src/modules/conventions/` (new module wiring the existing rails).
- **2026-07-09** — The resolve-within-clone path-traversal guard is INTENTIONALLY duplicated in two places: `server/src/modules/reviews/helpers.ts` (`readDocWithinClone`, application layer) and `server/src/modules/repo-intel/service.ts` (`readDocContent`, infrastructure facade). Both reject absolute paths and anything resolving outside `resolve(clonePath)+sep`. They are NOT DRY-unified because a single shared helper would have to live in a place both a module-application-helper and the repo-intel infra facade may import — and the onion rule forbids module→module imports, while repo-intel must stay reachable only via its facade (server CLAUDE.md). Extracting to `platform/` was rejected to avoid making the composition root a general util dump. If you change one copy (e.g. add symlink-escape hardening), change BOTH and keep the logic identical. Evidence: `reviews/helpers.ts` (`readDocWithinClone`), `repo-intel/service.ts` (`readDocContent`).
- **2026-06-27** — An agent run is an IMMUTABLE prompt snapshot: `prompt_assembly.skills` in a saved trace reflects the skills linked + enabled AT RUN TIME, and linking/enabling a skill afterwards NEVER back-fills past runs. The executor reads `agents.linkedSkills(agentId)` fresh on each run, filters `skill.enabled`, and joins bodies by `order`. So "I attached a skill but don't see it in the assembly" almost always means the trace predates the attach (re-run to verify), not a broken link — check live link state with `GET /agents/:id/skills` + the skill's `enabled` before suspecting the wiring. Evidence: `server/src/modules/reviews/run-executor.ts:192`.
- **2026-06-27** — `prompt_assembly.skills` in the trace is a SINGLE concatenated STRING, not a structured array: `assemblePrompt` does `parts.skills.join('\n\n')` under one `## Skills / rules` header (`reviewer-core/src/prompt.ts:88,109`), and `ReviewInput.skills` is just `string[]` (bodies, no names). So per-skill labels/delimiters CANNOT be added in the client trace viewer (the boundaries are unrecoverable from the flattened text) — they must be embedded at assembly time. Done in `run-executor` by mapping each enabled linked skill to `### ${skill.name}\n\n${skill.body}` before the join (the engine contract stays `string[]`); this labels skills for BOTH the model and the trace view. Evidence: `server/src/modules/reviews/run-executor.ts:193`.

- **2026-06-28** — To let one module's flow reuse ANOTHER module's SERVICE without tripping the `no-cross-module-internals` arch rule, expose the service on the container (the composition root is exempt — no rule uses `^src/platform` as a `from`) and consume it via `this.container.<svc>` — a property access, NOT a static import edge. The rule's `to:` only matches sibling `service`/`repository` FILES, so importing a non-service helper like `settings/feature-models.ts` is fine, but `new ReviewRepository()` from another module is not — read shared data through the already-exposed `container.reviewRepo`. This is how the Intent Layer wires into the reviews executor (intent computed ONCE per PR in `executeRuns`, not per agent). Evidence: `server/src/platform/container.ts` (`get intent()`), `server/src/modules/intent/service.ts` (uses `container.reviewRepo`, never `new ReviewRepository`), `server/src/modules/reviews/run-executor.ts` (`buildIntent` → `container.intent`).

- **2026-07-05** — Blast Radius (`GET /pulls/:id/blast`, new layered `blast/` module) is another "rails already laid" feature (cf. skills/conventions): the engine `container.repoIntel.getBlastRadius(repoId, changedFiles)` was ALREADY implemented (`repo-intel/service.ts:220`, persistent-index path + ripgrep degraded fallback, callers capped at 20/sorted by rank) and the `BlastRadius` contract already existed (`vendor/shared/contracts/brief.ts:39`). So the module is thin orchestration + ZERO LLM: `reviewRepo.getPull` → `reviewRepo.getPrFiles` (`.map(f=>f.path)`) → `repoIntel.getBlastRadius` → reshape the FLAT `BlastResult` (changedSymbols/callers/impactedEndpoints/factsByFile) into the per-symbol tree `BlastRadius` (group callers by `viaSymbol`; attribute endpoints/crons via `factsByFile` of each symbol's caller files). Index status for the UI badge comes from `repoIntel.getIndexState` — map its synthesized degraded row (`degradedReason` `no_data`/`flag_off`) to a `'none'` status so the client shows an empty state, not a scary badge. Evidence: `server/src/modules/blast/{service,helpers,routes}.ts`, `vendor/shared/contracts/review-api.ts` (`BlastRadiusResponse`).
- **2026-07-05** — Blast Radius pulls from TWO sources, not just the index: the impact MAP (symbols/callers/endpoints/crons) comes from `container.repoIntel`, but "prior PRs touching these files" comes from the app's own PR history (`pull_requests`⋈`pr_files`) via `container.reviewRepo` — NOT from repo-intel. So a repo that is unindexed (`status: 'none'`, empty map) can still have prior PRs, and vice-versa. The query is a `selectDistinct` over the PR identity joined to `pr_files` filtered by the changed paths + `ne(id, currentPr)`, scoped by `workspaceId`+`repoId` (`priorPrsTouchingFiles`). Lives on `ReviewRepository`/`pull.repo.ts` (which OWNS those tables) rather than a new blast repository, per the "read a sibling table through the owning container repo" rule. Evidence: `server/src/modules/reviews/repository/pull.repo.ts` (`priorPrsTouchingFiles`), `server/src/modules/blast/service.ts`.
- **2026-07-09** — Project Context (SPEC-01) is the specs-slot twin of the skills wire: the engine already renders `## Project context` from `ReviewInput.specs?: string[]` (`assemblePrompt` wraps each entry `wrapUntrusted('spec-<i>', body)` under `INJECTION_GUARD`, omits when empty), so the only server work was one injection point in `run-executor.runOneAgent`, right beside the skills block. Two gotchas that bit: (1) the SkillsRepository was NOT on the container (only instantiated inside `skills/service.ts`), so reaching skill-inherited docs required ADDING `get skillsRepo()` to `container.ts` — this is composition-root wiring of an EXISTING repo (the "expose service/repo on the container" pattern), not a new repo. Agent docs come from the already-exposed `this.agents.linkedContextDocs`. (2) D2 merge order = each ENABLED linked skill's docs (skill order, then that skill's doc order) FIRST, then agent-attached docs, deduped by repo-relative path keeping the FIRST (skill) occurrence — so a doc on both keeps its earlier skill slot. Paths are read FRESH per run from `repo.clonePath` (immutable-snapshot rule). Evidence: `server/src/modules/reviews/run-executor.ts` (specs block), `server/src/platform/container.ts` (`get skillsRepo()`), `server/test/context-docs.it.test.ts`.
- **2026-07-09** — Run-time doc reads for the specs slot go through `readDocWithinClone(clonePath, relPath)` in `reviews/helpers.ts` — a REQUIRED path-traversal guard (OWASP A01/A05) because attachment paths are repo content (attacker-influenceable). It rejects absolute paths and any path that, once `resolve(clonePath, relPath)`'d, is not strictly within `resolve(clonePath) + sep` (blocks `../escape` AND a prefix-sibling like `<root>-evil/…`), returning the file text or `null` on ANY failure (traversal/missing/unreadable). Callers OMIT a `null` doc and continue — an unreadable/deleted/traversal doc never fails the run and never reaches the prompt (AC-12). This hardens the plain `readClone` join at `repo-intel/service.ts:786` (which has NO within-clone assertion). Doc TEXT must NOT be logged (secret-redacting logs); it lives only in the trace's `prompt_assembly.specs`. Evidence: `server/src/modules/reviews/helpers.ts` (`readDocWithinClone`), `server/test/context-docs.it.test.ts` (deleted-doc + `../ctx-secret.md` cases).
- **2026-06-28** — Smart Diff (`GET /pulls/:id/smart-diff`) is a DETERMINISTIC compose with NO model call — the expensive LLM work already happened in the Structured Reviewer. It classifies each changed file PATH-ONLY into core/wiring/boilerplate (`classifier.ts`, precedence boilerplate→wiring→core so a generated `dist/index.js` beats the `index.*` wiring rule) and overlays the LATEST review's finding lines, computed ON READ — it does NOT persist to `pr_brief`, and `SmartDiffFile.pseudocode_summary` stays `null` (that's the LLM field, intentionally unfilled this lesson). Added inline in the thin `pulls` module (consistent with its grandfathered `route-direct-db-legacy` warn, not a new layered module). Patterns + the `BIG_PR_LINE_THRESHOLD` split heuristic live in `classifier.constants.ts`. Evidence: `server/src/modules/pulls/{classifier,classifier.constants,smart-diff,routes}.ts`.
- **2026-07-10** — The D2 context-doc merge (enabled skill-inherited docs first, then agent-attached, dedup keeping first) is NO LONGER inline in `run-executor.runOneAgent` (SUPERSEDES the 2026-07-09 "done inline" note): it was extracted into `AgentsService.resolveContextDocPaths(agentId)` and exposed via a NEW `container.agents` service getter (mirrors `container.intent`), so BOTH the reviews run-executor AND the new `brief` service resolve an agent's docs through the SAME code — they cannot drift on which docs an agent contributes (a drift would inject different project context into the brief vs the review). When adding any other consumer of an agent's context docs, call `container.agents.resolveContextDocPaths`; do not re-implement the merge. Evidence: `server/src/modules/agents/service.ts` (`resolveContextDocPaths`), `server/src/platform/container.ts` (`get agents()`), `server/src/modules/reviews/run-executor.ts` + `server/src/modules/brief/service.ts` (both call it).
- **2026-07-10** — The `pr_brief` table (previously unused — cf. the Smart Diff note that it "does NOT persist to pr_brief") is now wired by the Why+Risk Brief feature as a single-row-per-PR JSONB cache: the WHOLE stored object (`BriefStored` = the LLM `Brief` + `head_sha` + `generated_at` + model/cost/tokens + an `inputs` reproducibility snapshot) lives inside the one `json` column — NO new column, NO migration. `getBrief`/`upsertBrief` (`onConflictDoUpdate` on `pr_id`) mirror `getIntent`/`upsertIntent` on `ReviewRepository` (both places — see the completeAgentRun two-places caveat). Staleness is DERIVED, not stored: `outdated` = stored `head_sha` ≠ current `pull_requests.head_sha`, computed on READ in the GET path (no auto-regen/invalidate). Model chosen via `resolveFeatureModel(container, ws, 'risk_brief')` (already-registered FeatureModelId), never hardcoded. Reusable pattern for any future per-PR cached LLM artifact: put version/observability/input fields INSIDE the jsonb, derive freshness by comparing a stored `head_sha`. Evidence: `server/src/modules/reviews/repository/pull.repo.ts` (`upsertBrief`/`getBrief`), `server/src/modules/brief/service.ts`, `server/src/vendor/shared/contracts/review-api.ts` (`BriefStored`).
- **2026-07-11** — SUPERSEDES the 2026-06-28 "`pseudocode_summary` stays null" note: the per-file "What this does" summary is now populated by a NEW LAYERED module `diff-summary` (`POST /pulls/:id/smart-diff/summaries`, rate-limited), which makes ONE batched `completeStructured` call over the CORE-group files only (cap 15, `diff_summary` FeatureModelId, patches untrusted-wrapped + truncated) and caches `path → { hunk_hash, summary }` in a new `pr_diff_summary` jsonb table (mirrors `pr_brief`). Two reusable rules: (1) the LLM WRITE path is a proper layered module — do NOT add the LLM call to the thin `pulls` module; the thin `GET /smart-diff` only READS the cache inline (a Drizzle select, consistent with its grandfathered warn) and fills `pseudocode_summary`. (2) Per-file staleness uses a SHARED `hunkHash(patch)` (sha1, `pulls/hunk-hash.ts`) imported by BOTH the generator AND the GET reader — the GET fills a summary only when the cached `hunk_hash` matches the file's CURRENT patch hash, else leaves `null`, so a summary auto-invalidates when that file's diff changes (new commits) with no version column. Change the hash fn → both sides must change together. Evidence: `server/src/modules/diff-summary/{service,routes,helpers}.ts`, `server/src/modules/pulls/hunk-hash.ts`, `server/src/modules/pulls/routes.ts` (smart-diff GET reconcile), `server/src/db/schema/reviews.ts` (`prDiffSummary`), migration `0013_lumpy_magma.sql`.

## Tool & Library Notes

- **2026-06-14** — New DB columns: edit `db/schema/*.ts`, then `npm run db:generate` (drizzle-kit) auto-generates `00NN_*.sql` (e.g. `0010_solid_baron_zemo.sql` = `ALTER TABLE … ADD COLUMN`). Never hand-write migration SQL; apply with `npm run db:migrate`.
- **2026-06-27** — Widening a Drizzle `text({ enum: [...] })` column generates NO migration — the DB type stays `text`, the enum list is a TS-only compile-time narrowing. So adding e.g. `'imported_file'` to `skills.source` needs only the schema edit + both vendored `SkillSource` contracts in lock-step; `db:generate` produces nothing. (Contrast a real pg `enum` type, which WOULD need an `ALTER TYPE`.) Evidence: `server/src/db/schema/skills.ts`.
- **2026-06-27** — Onion layering is machine-enforced: `pnpm arch:check` runs `dependency-cruiser` against `server/.dependency-cruiser.cjs` (also an `arch` job in `.github/workflows/server-unit.yml`). Error-severity rules fail CI; 6 known warns are grandfathered debt (4 thin-module routes + 2 repo-intel service→adapter) — keep errors at 0, don't grow the warns. Config sets `tsPreCompilationDeps: false` ON PURPOSE: counting `import type` edges flags harmless cycles (helpers⇄repository on a row type; container⇄service DI root). `dependency-cruiser` was already a dep (repo-intel indexer) — reused, not added. Evidence: `server/.dependency-cruiser.cjs`.
- **2026-06-27** — `container.repoIntel.getConventionSamples(repoId, n)` returns only top-N ranked file PATHS (tests/configs/migrations stripped), NOT file contents and NOT config files — despite the "conventions sample" name implying configs + code. To build an extraction sample you must read the paths' contents yourself via `readFile(join(repo.clonePath, relPath),'utf8')` (the indexer pattern at `repo-intel/pipeline/incremental.ts:154`) AND add config files (`tsconfig.json`, `.eslintrc*`, `.prettierrc*`, `package.json`) separately. Evidence: `server/src/modules/repo-intel/service.ts:630` (delegates to `getTopFilesByRank`).
- **2026-06-27** — Provider API keys are NOT (only) in `server/.env` — `LocalSecretsProvider` reads `~/.devdigest/secrets.json` (UI "BYO key" writes, mode 0600) FIRST, falling back to `process.env`. `container.llm(provider)` throws `ConfigError('<PROVIDER>_API_KEY is not configured')` when that provider's key is absent in both. Gotcha for any system LLM feature: a `FeatureModelId`'s `defaultProvider` may differ from the providers the user actually has keys for (e.g. `conventions` defaults to `openai/gpt-5.4`, but a user with only Anthropic+OpenRouter keys gets a hard ConfigError until they pick a different model in Settings → Feature Models). To debug "which keys exist", inspect `~/.devdigest/secrets.json`, not `.env`. Evidence: `server/src/adapters/secrets/local.ts`, `server/src/platform/container.ts:175-191`, `vendor/shared/contracts/platform.ts` (FEATURE_MODELS defaults).

## Recurring Errors & Fixes

- **2026-06-14** — Adding a required field to a Zod contract (`RunStats.cost_usd`) breaks the inline fixture in `server/test/contracts.test.ts` (RunTrace parse). Update the `stats: {…}` fixture in the same change. Evidence: `server/test/contracts.test.ts:160`.
- **2026-06-28** — Per-package npm scripts (e.g. `verify:l03`, `test`, `typecheck`) MUST be run from inside the package dir — `cd server && pnpm verify:l03`, or `pnpm -C server verify:l03` from elsewhere. Running `pnpm <script>` from the repo ROOT fails with `ERR_PNPM_NO_PKG_MANIFEST: No package.json found in /…/dev-digest` because this is NOT a pnpm workspace — there is no root `package.json`/`pnpm-workspace.yaml`; each package (`server/`, `client/`, …) has its own manifest. Evidence: `server/package.json` (scripts live here), repo root has no manifest.
- **2026-07-09** — `db:generate` numbers the migration off the CURRENT tree's existing migrations, so generating from the wrong base collides. The `implementer` subagent (Agent `isolation:worktree`) cuts its worktree from `main`, but course work lives on the `lesson5` branch (main is 13 commits behind). Its `db:generate` saw migrations only up to `0010` and emitted `0011_*` — colliding with lesson5's real `0011_wise_dakota_north.sql`, and its drizzle snapshot lacked all lesson5-only tables. Fix: never trust a migration generated in a worktree cut from a stale `main`; re-run `db:generate` on the actual feature branch (produced the correct `0012_slippery_talon.sql`). More generally, use worktree-isolated implementers only when `main` is current with the working branch — otherwise implement in-tree. Evidence: `server/src/db/migrations/0012_slippery_talon.sql`.
- **2026-07-09** — Editing a `server/src/vendor/shared/**/*.ts` contract is NOT enough: the server loads the committed sibling `.js` at runtime. `server/vitest.config.ts` aliases `@devdigest/shared` → `src/vendor/shared`, and Node/vite resolve the literal `./contracts/x.js` import specifier, so a `.ts`-only edit passes `tsc --noEmit` but runtime uses the STALE `.js` (symptom: `SettingsKnown.parse({})` returns the old key set → `undefined` → 500 `reading 'safeParse'/'filter' of undefined`; a value-import from a `.ts` with no sibling `.js` throws module-not-found). Fix: regenerate the sibling `.js`(+`.js.map`) in place — `cd server && ./node_modules/.bin/tsc <changed .ts files> --outDir src/vendor/shared --rootDir src/vendor/shared --module esnext --target es2022 --moduleResolution bundler --sourceMap --declaration false`. The CLIENT `vendor/shared` ships NO `.js` (Next compiles `.ts` directly), so the client copy needs only the `.ts` edit. Evidence: `server/vitest.config.ts` alias; `server/src/vendor/shared/contracts/context.js` (regenerated).
- **2026-07-11** — An integration test for an LLM feature whose `FeatureModelId` defaults to `openrouter` (e.g. `diff_summary`, `review_intent`) will silently make a REAL network call — flaking/spending — if a real `OPENROUTER_API_KEY` happens to be configured on the machine (`~/.devdigest/secrets.json` OR env), because the test seeds that default model and `container.llm('openrouter')` then resolves a LIVE client instead of the `MockLLMProvider`. Symptom: a hermetic-looking test intermittently hits the network / burns tokens depending on the dev's local keys. Fix: PIN the workspace's feature model to the mock-backed provider inside the test (`PUT /settings` with `feature_models: { <feature>: { provider, model } }` matching whatever provider the test registered `MockLLMProvider` under, e.g. `openai`) so the run is hermetic regardless of local keys. Evidence: `server/test/diff-summary.it.test.ts` (per-test model pin in `makeApp`).

## Session Notes

### 2026-06-14
- Re-introduced per-run cost (USD) end-to-end (lesson reversing the earlier removal in `d45ab0d`/`58c6ac7`): `cost_usd` column on `agent_runs` (migration 0010), captured in `run-executor` (was discarding `outcome.costUsd`), surfaced in `RunSummary`/`RunStats`/`PrMeta`.
- Decision: PR-list COST = sum of the latest review batch via a 120s window heuristic (no batch id in schema). Cost persisted (accurate `outcome.costUsd`), not recomputed; historical runs → null → "—".

## Open Questions

- **2026-06-14** — PR-list "latest review batch" uses a 120s `ranAt` window as a proxy for a review session. If a real review-session / batch id is ever added to the schema, swap the window for exact grouping in `pulls/routes.ts`.
