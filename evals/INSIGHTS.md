# Insights — evals

Non-obvious findings and gotchas. Add an entry whenever something surprised you,
so the next agent/session doesn't relearn it. Append-only — see the
`engineering-insights` skill for how entries are captured.

## What Works

- **2026-07-15** — To A/B two *different* artifacts (e.g. a strict agent vs a relaxed variant) rather than artifact-on/off, give each its own `*.eval.ts` + `describeAgent(name)` but have both `import { cases }` from ONE shared `.cases.ts`, so the two agents are graded on identical prompts/practices/thresholds. Run `eval:repeat` on each with a distinct `--label`, then `eval:delta`. Evidence: `agents/architecture-reviewer-lite/architecture-reviewer-lite.eval.ts` reuses `architecture-reviewer/architecture-reviewer.cases.ts`.
- **2026-07-15** — You can recover a clean A/B from a contaminated `results/repeat-*.json` WITHOUT re-running any model: the raw rows survive in `results/records.jsonl` tagged by `run_id`, and the `repeat-*.json` `tests` object is keyed by full nodeid — filter its keys (e.g. `.includes("architecture-reviewer")`) and re-run `eval:delta`. Saves the token cost of a re-run when only the aggregation was polluted.

## What Doesn't Work

- **2026-07-15** — `eval:repeat` originally attributed "its" records by LINE OFFSET into the shared append-only `results/records.jsonl` (`loadRecords(startLine)`), so ANY other eval process writing concurrently leaked into the labeled series. A parallel `dependency-checker` run (separate `run_id`, timestamped mid-window) dumped 5 of its cases into `repeat-strict.json` and 2 into `repeat-lite.json`, and `delta` then rendered them as noise rows. Fix: tag each repeat invocation (`EVAL_RUN_TAG=repeat-<pid>-<ts>`), forward it to every vitest child, stamp `run_tag` on each record, and filter by tag instead of offset. Lesson: a shared global log + line-offset windowing is not concurrency-safe. Evidence: `src/repeat.ts` (runTag/`mine()`), `src/records/record.ts` (`RUN_TAG`).
- **2026-07-15** — `eval:delta` aligns two series by FULL nodeid, which is `${testPath} > ${describePrefix} > ${testName}` (`src/records/record.ts:45`). A cross-artifact A/B differs in BOTH the file path AND the describe prefix (`agent:architecture-reviewer` vs `…-lite`), so the union of keys is disjoint and every practice renders one-sided (`100% -> —%`) — the promised "which practice moved" never lines up. Fix: key delta on the trailing `" > "` segment (the `test()` name, deliberately shared by an A/B pair); same-file baseline/candidate still aligns since the trailing name is identical there too. Evidence: `src/delta.ts` (`shortKey`/`byShort`).
- **2026-07-15** — A `kind:"activation"` near-miss (`shouldActivate:false`) is only winnable against an EVENT/TASK-triggered skill; against a TOPIC-triggered one it is unwinnable. `onion-architecture`'s SKILL.md lists bare topic terms ("Trigger terms: onion architecture, clean architecture, layering…"), so the pure-explanation prompt "поясни загалом, що таке onion architecture" activates it — confirmed on a run (`skills: onion-architecture`, 0 files read). Contrast `engineering-insights`, which fires on the EVENT ("щойно з'ясував…"), so "explain pgvector dimensions" is a valid negative. Author activation negatives only for event/task-triggered skills; otherwise drop the negative and keep the positive. Evidence: `.claude/skills/onion-architecture/SKILL.md` (Trigger terms), `workflow/review-workflow.cases.ts`.
- **2026-07-15** — A workflow-tier `expectFilesRead`/`expectSubagents`/`skill` asserts against the REAL on-disk repo (`workflowTask` → `settingSources:["project"]`), so a target that doesn't exist is a guaranteed red, not a harness miss — and it fails only at run time, never at collect/typecheck. The pre-existing `workflow:review` cases asserted reads of `server/docs/api-contracts.md`, `reviewer-core/docs/pipeline.md`, `reviewer-core/insights/gotchas.md` (none exist) and even routed pipeline to `pipeline.md` though `reviewer-core/CLAUDE.md` routes the pipeline overview to `README.md`. Verify every asserted path/agent/skill exists AND that CLAUDE.md actually routes there before writing the case. Evidence: `workflow/review-workflow.cases.ts`, `src/tasks.ts` (`workflowTask`), `reviewer-core/CLAUDE.md` (Use when).
- **2026-07-15** — The eval CI change-detector (`scripts/ci-detect.mjs`) mapped a guide edit to the workflow tier via `f === "CLAUDE.md"`, but repo-root `CLAUDE.md` is a **symlink to `AGENTS.md`** — a real edit to the guide lands in the PR diff as `AGENTS.md`, so the workflow tier silently never fired. Fix: match BOTH `CLAUDE.md` and `AGENTS.md` in the detector, and list both in `eval-workflow.yml`'s `paths:` trigger. Any CI path/name filter over the guide must watch the symlink TARGET, not the link. Evidence: `scripts/ci-detect.mjs` (`runWorkflow`), `.github/workflows/eval-workflow.yml`.
- **2026-07-16** — Routing an **eval-only A/B "B-side" agent into a CI gate is a guaranteed red**. `architecture-reviewer-lite` has the rule-citation requirement removed, so the shared citation case (`threshold: 1.0`) scores ~0.67 BY DESIGN — that low score IS the A/B finding, not a regression. First CI run failed exactly there. Fix: `ci-detect.mjs` keeps an `EVAL_ONLY_AGENTS` set that is excluded from the gated agent list and logged as "SKIP (eval-only A/B, measure manually)"; measure it via `eval:repeat`+`eval:delta`, never a pass/fail gate. Evidence: `scripts/ci-detect.mjs` (`EVAL_ONLY_AGENTS`), `agents/architecture-reviewer/architecture-reviewer.cases.ts:61` (threshold 1.0).
- **2026-07-16** — Excluding an eval from the gated NAME list is not enough — `ci-detect.mjs` first emitted a DIRECTORY arg (`agents/architecture-reviewer`) as the vitest path list, and vitest treats a positional arg as a SUBSTRING filter, so it ALSO matched `agents/architecture-reviewer-lite/…` and silently re-ran the excluded A/B B-side in CI (same substring trap `eval:repeat` documents). Fix: emit the EXACT `*.eval.ts` file paths (`agents/<name>/<name>.eval.ts`) — a full file path can't substring-match a sibling `-lite/` dir. Evidence: `scripts/ci-detect.mjs` (`evalFiles`, `skills_paths`/`agents_paths`).
- **2026-07-16** — SUPERSEDES the "mutating tools are stripped" clause of the `agentTask` note in Codebase Patterns: stripping only removes them from the ALLOW-list, which does NOT keep them out. `run-claude` uses `permissionMode:"bypassPermissions"`, under which a built-in like `Bash` is still callable even when absent from `allowedTools`. Proof: the architecture-reviewer agent-tier eval called `Bash` to run `pnpm arch:check` — green locally (server deps present) but RED in the evals-only CI job (`depcruise: not found` → the agent turned the tool failure into a fabricated gate FAIL). Also unsafe: a leaked `Bash` on the live repo under bypass could mutate. Fix: pass a real SDK deny-list — `agentTask` now sets `disallowedTools:[...MUTATING_TOOLS]`, which holds under bypass. Verified: every trace then shows `tools:(none)`, all 4 strict cases stay green, and the tier is hermetic (no per-package toolchain needed). Evidence: `src/runtime/run-claude.ts` (`disallowedTools` in `RunOptions`/`Options`), `src/tasks.ts` (`agentTask`), `src/artifacts/load.ts` (exported `MUTATING_TOOLS`).

## Codebase Patterns

- **2026-07-15** — `agentTask`/`runAgentCases(name, …)` load the agent-under-test from **repo-root** `.claude/agents/<name>.md` by that exact `name` (not the eval directory name) and inject its frontmatter-stripped body as the system prompt. The agent's declared `tools:` are honored (mutating tools — Write/Edit/Bash/NotebookEdit — are stripped, `*` collapses to Read/Grep/Glob), but its `model:` frontmatter is IGNORED: every run uses `EVAL_MODEL` (default `claude-haiku-4-5`), judged by `EVAL_JUDGE_MODEL` (default `claude-sonnet-5`). So a missing/misnamed agent file fails only at run time with `agent not found`, and setting `model:` on the agent has no effect on the eval. Evidence: `src/artifacts/load.ts` (`agentContent`/`agentTools`), `src/tasks.ts` (`agentTask`), `src/runtime/run-claude.ts`, `src/config.ts`.
- **2026-07-15** — `architecture-reviewer` / `architecture-reviewer-lite` are **eval-only** A/B agents, NOT wired into production (the production reviewer is `arch-reviewer`). `-lite` is byte-for-byte the strict variant minus two output-discipline hard rules: (1) cite the documented rule id per finding (+ the "drop a finding you can't map to a rule" clause that rides with it), (2) quote the offending line verbatim. The cases pair a TEXTBOOK-violation diff (non-discriminating — the model volunteers `inward-only-dependencies`/`di-discipline` even without the rule) with a DevDigest-SPECIFIC diff (`reviewer-core-zero-io`, `reviewer-core-ground-findings-gate`) that only the citation rule reliably surfaces; that is why removing citation shows 100→0 on reviewer-core but 100→100 on checkout. Evidence: `.claude/agents/architecture-reviewer*.md`, `agents/architecture-reviewer/architecture-reviewer.cases.ts`.
- **2026-07-15** — CI eval tiers split by whether they need the LiteLLM proxy. The **skills** tier is content-only (`skillTask` + the LLM judge both go through `runContent` → `run-openrouter.ts`, a direct OpenAI-format call), so it hits OpenRouter natively and needs **no proxy** — `eval-skills.yml` runs proxy-free and defaults to DeepSeek. The **agents** and **workflow** tiers run inside the Agent SDK (Anthropic wire protocol), so they MUST route through the proxy (`OPENROUTER_BASE_URL=http://localhost:4000`) and default to `google/gemini-2.5-flash` (DeepSeek won't dispatch subagents on the workflow tier). Bring the proxy up only when there's work to run. Evidence: `src/runtime/dispatch.ts`, `src/runtime/run-openrouter.ts`, `.github/workflows/eval-agents.yml`, `.github/workflows/eval-workflow.yml`.
- **2026-07-16** — SUPERSEDES the workflow-tier half of the note above. On Gemini the workflow tier is only **2/4** (activation + two-hop doc routing degrade — the cheap model does the work inline / stops at maxTurns instead of invoking the Skill or hopping docs), i.e. "indicative, not blocking". `anthropic/claude-haiku-4.5` is **4/4** and gate-worthy. Because `anthropic/*` slugs are served natively by OpenRouter's **Anthropic Skin**, haiku needs **NO proxy** — the SDK hits `https://openrouter.ai/api` via `env.ts`'s default when `OPENROUTER_BASE_URL` is UNSET. So `eval-workflow.yml` now defaults to haiku and drops all Docker/proxy steps; the proxy is only for non-Anthropic tool-tier models (`eval-agents.yml`). Evidence: `.github/workflows/eval-workflow.yml`, `src/runtime/env.ts` (base-URL default), `evals/README.md:106` (Skin example).
- **2026-07-16** — `kind:"activation"` workflow cases GATE on the subscription path but are INDICATIVE (logged `[indicative activation] …`, non-blocking) under `EVAL_BACKEND=openrouter` (`ACTIVATION_INDICATIVE` in `src/dsl/case.ts`). Reason: the Skill tool fires on the Claude Code subscription harness (verified: `tools:[Skill,Read]`, `skills:[onion-architecture]`, reads `rules/adding-a-module.md`) but over the OpenRouter/Agent-SDK gateway even haiku answers by reading the module tree directly (`agents/routes.ts`, `service.ts`, `container.ts`) and never invokes Skill — so the activation positive is unwinnable in CI regardless of model. CI therefore can't gate on activation; the 3 deterministic `trace` doc-routing cases stay hard gates on both paths. Also: `workflowTask` now denies `MUTATING_TOOLS` — `Bash` was leaking via bypassPermissions against the live `settingSources:["project"]` checkout. Evidence: `src/dsl/case.ts` (`ACTIVATION_INDICATIVE`), `src/tasks.ts` (`workflowTask` disallowedTools).

## Tool & Library Notes

- **2026-07-15** — `eval:repeat` hard-caps `-n` at 2 (`MAX_TIMES`, token economy) — pass more and it silently clamps. A bare directory arg like `agents/architecture-reviewer` is a vitest SUBSTRING that also matches `agents/architecture-reviewer-lite/…`; `resolveEvalPatterns` guards this by expanding a dir arg to its exact `.eval.ts` file paths, but ONLY if the arg resolves as a directory relative to the repeat process cwd — so invoke via `pnpm -C evals eval:repeat agents/<name>` (cwd = evals), not from repo root, or the expansion silently no-ops and the A/B doubles up. Evidence: `src/repeat.ts` (`MAX_TIMES`, `resolveEvalPatterns`).

## Recurring Errors & Fixes

## Session Notes

### 2026-07-15
- Built a controlled A/B for the architecture reviewer: created eval-only `architecture-reviewer` (strict) and `architecture-reviewer-lite` agents (the two `*.eval.ts` + shared `.cases.ts` + fixtures were already committed but the agent artifacts were missing, so the evals would have failed with `agent not found`).
- Fixed two framework gaps surfaced by the A/B: `delta` now aligns on the trailing test-name segment (cross-artifact A/B), and `repeat` now isolates its records by a per-invocation `run_tag` instead of a line offset into the shared log.
- A/B result (haiku under test, sonnet judge, n=2): removing the citation rule collapsed DevDigest-specific rule-id naming 100→0 but left textbook rule naming untouched; removing the verbatim rule degraded evidence 100→50; fabrication-resistance on an out-of-scope diff dropped 100→50; detection, severity, and the PASS/FAIL gate were unaffected.
- Rebuilt the `workflow:review` tier as CLAUDE.md-convention checks (session-protocol INSIGHTS read, two "Read When" doc routes, `onion-architecture` activation): 4/4 green on haiku. Dropped an `onion-architecture` near-miss negative after a run proved the skill is topic-triggered, so a pure-explanation prompt legitimately activates it. All asserted targets verified to exist first, unlike the prior cases which pointed at absent docs.
- Deployed the harness evals to GitHub Actions: three PR-triggered workflows (`eval-skills` / `eval-agents` / `eval-workflow`) reusing `ci-detect.mjs` for per-artifact change detection + skip-when-no-evals. Added `skills_paths`/`agents_paths` detector outputs (space-joined vitest paths, so no `jq` in bash) and fixed the `CLAUDE.md`→`AGENTS.md` symlink gap. Models switchable via `workflow_dispatch` inputs; skills default DeepSeek (proxy-free), agents+workflow default Gemini Flash (via proxy); tool tiers run `--retry=1 --no-file-parallelism` for OpenRouter flakiness.

## Open Questions
